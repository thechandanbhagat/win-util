# Spotlight generators

When adding or changing a Spotlight generator, create or update its dedicated Settings sidebar page in the same change. Persist every relevant output option and use the saved setting when generating the result. Do not wait for an explicit request for settings unless the user states that the generator must have no configuration.
